import React from 'react';

import styles from './ServerComment.module.css';

export interface ServerCommentProps {}

function ServerComment({}: ServerCommentProps) {
  return <div className={styles.wrapper}></div>;
}

export default ServerComment;
